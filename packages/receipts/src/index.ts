/**
 * @paykit/receipts — verifyReceipt(signedJws, …) ships in Phase 4.
 */
export function verifyReceipt(_signedJws: string, _opts?: unknown): never {
  throw new Error("@paykit/receipts: not implemented (Phase 4)");
}
