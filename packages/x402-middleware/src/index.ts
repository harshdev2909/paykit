/**
 * @paykit/x402-middleware — Express / Next.js paywall helpers ship in Phase 4.
 */
export function paywall(_opts?: unknown): never {
  throw new Error("@paykit/x402-middleware: not implemented (Phase 4)");
}
