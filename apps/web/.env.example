# PayKit HTTP API base URL (browser + server). Production: https://paykit-1.onrender.com
# The /demo “Allowed:” chip uses this hostname (or NEXT_PUBLIC_DEMO_ALLOWED_HOST).
NEXT_PUBLIC_PAYKIT_API_URL=http://127.0.0.1:3001
# Optional: override the “Allowed:” label on /demo (echo paywall host) if it differs from the API URL.
# NEXT_PUBLIC_DEMO_ALLOWED_HOST=paykit-1.onrender.com

# Same host as the API for OAuth and checkout (optional if identical to PayKit URL).
NEXT_PUBLIC_API_URL=http://127.0.0.1:3001

# Optional: "Edit on GitHub" links in docs footer (path to `app/(site)/docs`, no trailing slash).
# NEXT_PUBLIC_DOCS_GITHUB_EDIT_BASE=https://github.com/your-org/paykit/blob/main/apps/web/app/(site)/docs

# Demo (/demo): server-side merchant API key + optional internal API URL for Next route handlers.
# PAYKIT_INTERNAL_API_URL=http://127.0.0.1:3001
PAYKIT_DEMO_MERCHANT_API_KEY=pk_test_your_demo_merchant_key

# Cloudflare Turnstile (first prompt only). Omit TURNSTILE_SECRET_KEY to skip verification in development.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# Optional: authorize Vercel Cron `/api/cron/keep-alive` with `Authorization: Bearer …`
# CRON_SECRET=
